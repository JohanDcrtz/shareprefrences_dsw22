import 'package:flutter/material.dart';

class RepuestosScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Pantalla de Repuestos'),
      ),
      body: Center(
        child: Text('Contenido de la pantalla de Repuestos'),
      ),
    );
  }
}
